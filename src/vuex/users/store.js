import * as types from '../mutation-types'
import * as token from '../../utils/token'

// initial module state
const state = {
	user: {}
}

// mutations
const mutations = {
	
}

export default {
  state,
  mutations
}
